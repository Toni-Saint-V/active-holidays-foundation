name: Actions Scheduler Smoke

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  smoke:
    name: Smoke
    runs-on: ubuntu-latest
    steps:
      - name: Prove scheduler can create a job
        run: echo "ok"

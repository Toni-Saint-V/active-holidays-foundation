import { ScreenPlaceholder } from "@/screens/shared/ScreenPlaceholder";

export function ProfileScreen() {
  return (
    <ScreenPlaceholder
      eyebrow="Profile"
      title="Профиль и история кейсов"
      description="Структура профиля готова. Следующим этапом подключим сохранённые сигналы и case history."
    />
  );
}
